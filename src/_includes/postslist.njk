{# <ul class="container list-none">
  {% for post in postslist %}
    {% if post.data.locale == locale %}
      <li class="column-1 my-5">
        <span class="first-letter:uppercase font-semibold text-lg">{{ post.data.date | formatDate(locale) }}</span>
        <h2 class="text-primary-color text-3xl my-2"><a class="underline decoration-primary-color font-bold" href="{{ post.url | url }}">{{ post.data.title }}</a></h2>
        <span class="text-lg">{{post.data.description}}</span>
      </li>
    {% endif %}
  {% endfor %}
<ul> #}

<ul class="list-disc px-8">

  {% for post in postslist %}
    <li class="mb-8">
      <div>{{ post.data.date }}</div>
      <h2 class="text-2xl text-violet-700 mb-1">
        <a class="decoration-violet-700 underline decoration-solid" href="{{ post.url | url }}">{{ post.data.title }}</a></h2>
      <div>{{post.data.description}}</div>
    </li>
  {% endfor %}
</ul> 